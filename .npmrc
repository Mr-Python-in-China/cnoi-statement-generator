# for @myriaddreamin/vite-plugin-typst becase the peer dependencies
# in the package only support vite v6, but it works fine with vite v7
# it may be removed in the future when the package is updated
legacy-peer-deps = true 
